specVersion: "v1beta"
serviceId: "buss-manage2"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "buss-manage2-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
